export default function VersionPage() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1>Nordic Auto Care v21</h1>
      <p>Vercel install fix: Node 20, npm install instead of npm ci, backend aliases active.</p>
    </main>
  );
}
